# --- API KEYS ---
GOOGLE_API_KEY=your_google_api_key_here
GROQ_API_KEY=your_groq_api_key_here

# --- MAIN GENERATOR CONFIG (RAG & Question Rewriting) ---
# Options: google, groq, ollama
# LLM_PROVIDER=google
# LLM_MODEL_NAME=gemini-2.5-flash-lite

Recommended for Groq:
LLM_PROVIDER=groq
LLM_MODEL_NAME=moonshotai/kimi-k2-instruct-0905

# --- ROUTER CONFIG (Intent Classification & General Chat) ---
# Defaults to LLM_PROVIDER if not specified
ROUTER_PROVIDER=google
ROUTER_MODEL_NAME=gemini-2.5-flash-lite

ROUTER_PROVIDER=groq
ROUTER_MODEL_NAME=moonshotai/kimi-k2-instruct-0905

# --- SYSTEM SETTINGS ---
# Adjust based on document density
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# --- FAISS INDEX CONFIGURATION ---
# Index type: flat (exact), ivf (approximate), ivfpq (approximate + compressed)
# - flat: Brute-force search, 100% accuracy, slower for large datasets
# - ivf: Clustering-based search, ~96% accuracy with nprobe=8, 5x faster
# - ivfpq: Clustering + compression, ~92% accuracy, 10x faster, 50% memory
VECTOR_INDEX_TYPE=flat

# IVF parameters (only used when VECTOR_INDEX_TYPE=ivf or ivfpq)
# IVF_NLIST: Number of clusters (recommend sqrt(n_vectors) to 4*sqrt(n_vectors))
# IVF_NPROBE: Clusters to search (higher = more accurate but slower)
IVF_NLIST=64
IVF_NPROBE=8
